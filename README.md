# Wallet-saver-starter---React-single-file-
a ready-to-use starter React wallet webapp in the canvas titled Wallet-saver-starter - React (single-file). It includes:  A single-file React component (default export) you can drop into a new CRA/Vite project.  Monthly income, category allocation, transactions, and savings-goal features.  localStorage persistence + import/export JSON. 
